nodes:
    - host: localhost
      port: 30001
    - host: localhost
      port: 30002
    - host: localhost
      port: 30003
    - host: localhost
      port: 30004

clients:
    - host: localhost
      port: 20030

loss%: 0

ckpt_interval: 1

retry_times_before_view_change: 2

sync_interval: 5

misc:
    network_timeout: 100